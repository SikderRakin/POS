import { FieldToDisplayPipe } from './field-to-display.pipe';

describe('FieldToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
